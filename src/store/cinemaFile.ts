// store/cinemaFile.ts

// let file: File | null = null
// let blobUrl: string | null = null

// export const setCinemaFile = (f: File) => {
//   file = f
//   blobUrl = URL.createObjectURL(f)
// }

// export const getCinemaFile = () => file
// export const getCinemaBlobUrl = () => blobUrl

// export const clearCinemaFile = () => {
//   if (blobUrl) URL.revokeObjectURL(blobUrl) // освобождаем память
//   file = null
//   blobUrl = null
// }
